{
  "name": "OM Screener Pipeline (TESTING - bypasses gate)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "om-screener/run-test",
        "responseMode": "onReceived",
        "options": {}
      },
      "id": "webhook-start-test",
      "name": "Webhook Start (Test)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "command": "cd \"{{ $env.CRE_ACQ_STAGE1 || '/path/to/cre-acquisitions-platform/stage-1-om-screener' }}\" && CMD=\".venv/bin/python3\"; [ -x \"$CMD\" ] || CMD=\"python3\"; \"$CMD\" src/run_pipeline.py --folder \"{{ $json.body.deal_folder }}\" --deal-id \"{{ $json.body.deal_id || 'deal' }}\" --skip-gate",
        "options": {}
      },
      "id": "run-pipeline-test",
      "name": "Run Pipeline (Test)",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [460, 300],
      "notes": "TESTING ONLY: bypasses human review gate. Production must use the full gated workflow (architecture.md)."
    }
  ],
  "connections": {
    "Webhook Start (Test)": {
      "main": [[{ "node": "Run Pipeline (Test)", "type": "main", "index": 0 }]]
    }
  },
  "active": false,
  "settings": {},
  "meta": {}
}
